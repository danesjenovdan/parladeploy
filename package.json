{
  "name": "parlameter-deploy",
  "version": "1.0.0",
  "main": "run.js",
  "license": "Unlicense",
  "scripts": {
    "deploy:hr:setup": "cross-env NODE_ENV=production pm2 deploy production_hr setup",
    "deploy:hr": "cross-env NODE_ENV=production pm2 deploy production_hr",
    "deploy:pl:setup": "cross-env NODE_ENV=production pm2 deploy production_pl setup",
    "deploy:pl": "cross-env NODE_ENV=production pm2 deploy production_pl",
    "deploy:english:setup": "cross-env NODE_ENV=production pm2 deploy production_english setup",
    "deploy:english": "cross-env NODE_ENV=production pm2 deploy production_english",
    "deploy:staging:sl:setup": "cross-env NODE_ENV=production pm2 deploy staging_sl setup",
    "deploy:staging:sl": "cross-env NODE_ENV=production pm2 deploy staging_sl",
    "parlanode:setup": "cross-env NODE_ENV=production pm2 deploy targets/parlanode/ecosystem.config.js production setup",
    "parlasite:setup": "cross-env NODE_ENV=production pm2 deploy targets/parlasite/ecosystem.config.js production setup"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "cross-env": "^5.2.0",
    "express": "^4.16.3",
    "flightplan": "^0.6.17",
    "lodash": "^4.17.10",
    "node-fetch": "^2.1.2"
  },
  "devDependencies": {
    "eslint": "4.19.1",
    "eslint-config-airbnb-base": "13.0.0",
    "eslint-plugin-import": "2.12.0"
  }
}
